{
  "name": "dd-bark",
  "version": "1.0.0",
  "description": "CLI tool for interacting with Datadog API — designed for AI agent usage",
  "type": "module",
  "bin": {
    "dd-bark": "./build/cli.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsup && node -e \"require('fs').chmodSync('build/cli.js', '755')\"",
    "dev": "tsup --watch",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@datadog/datadog-api-client": "^1.34.1",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@types/node": "^20.17.30",
    "tsup": "^8.4.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
